"use client";

import React from "react";
import { Info } from "./info";

export const Canvas = () => {
  return (
    <main className="h-full w-full bg-neutral-100 touch-none">
      <Info />
    </main>
  );
};
